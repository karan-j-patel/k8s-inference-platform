.DS_Store
*.env
.env*
*.key
*.pem
kubeconfig
secrets/
# Secret manifests are never committed. Real values come from
# `kubectl create secret` at apply time, or an external secret store.
# Structure goes in the README; values never go in git.
*secret*.yaml
*secret*.yml
!*sealedsecret*.yaml
